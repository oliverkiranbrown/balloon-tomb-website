import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  
  // catch case sensitivity issues before deployment
  reactStrictMode: true,
};

export default nextConfig;
  